rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /uploads/{userId}/{allPaths=**} {
      allow read: if request.auth != null &&
        (request.auth.uid == userId || request.auth.token.email in [
          'moranpe@gmail.com'
        ]);
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
